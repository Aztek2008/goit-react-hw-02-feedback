import React, { Components } from "react";
import "./App.css";

class App extends Components() {
  state = {
    good: 0,
    neutral: 0,
    bad: 0,
  };

  render() {
    return {
      App,
    };
  }
}

export default App;
